### ServerSocket / Socket과 포트번호

ClientApp -> Socket

Socket -> ServerApp -> ServerSocket
{{ 그림 }}

모든 소켓을 포트번호를 가지고 있다.

### Client 와 Server간 통신 규칙 정의

통신 규칙 : 프로토콜 (FTP , HTTP, Telnet, SMTP, POP3, IMAP 등)




